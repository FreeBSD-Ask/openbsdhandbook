# Web

Webservers pages:

- [Apache](apache.md)
  Installing and configuring the Apache HTTP Server on OpenBSD using the www/apache package, with TLS and virtual host support.
- [httpd](httpd.md)
  Using OpenBSD’s native httpd(8) daemon to serve static and FastCGI content over HTTP and HTTPS, with secure and minimal configuration.
- [nginx](nginx.md)
  Installing and configuring nginx on OpenBSD for static content, TLS, FastCGI, and reverse proxying.
- [relayd](relayd.md)
  Using relayd(8) on OpenBSD for TLS termination, reverse proxying, and HTTP load balancing with integrated monitoring and filtering.
